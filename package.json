{
    "name": "sonix20-monorepo",
    "private": true,
    "workspaces": [
        "obsidian-command"
    ],
    "scripts": {
        "build": "npm run build -w obsidian-command && rm -rf dist && cp -r obsidian-command/dist ./dist && rm -rf venv .venv voicebot-ui obsidian-command node_modules package-lock.json && du -sh * .[^.]* | sort -h"
    }
}