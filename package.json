{
    "name": "sonix20-monorepo",
    "private": true,
    "workspaces": [
        "obsidian-command"
    ],
    "scripts": {
        "build": "npm run build -w obsidian-command && rm -rf dist && cp -r obsidian-command/dist ./dist"
    }
}