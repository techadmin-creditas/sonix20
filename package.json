{
    "name": "sonix20-monorepo",
    "private": true,
    "scripts": {
        "build": "cd obsidian-command && npm install && npm run build",
        "install": "npm install && cd obsidian-command && npm install"
    }
}