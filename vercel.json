{
    "version": 2,
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "functions": {
        "api/index.py": {
            "includeFiles": "voicebot/**",
            "excludeFiles": "{obsidian-command/**,node_modules/**,voicebot-ui/**,venv/**,deploy_dist/**}"
        }
    }
}